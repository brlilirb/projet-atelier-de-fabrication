Il faut faire ça pour les classes qui seront accessible depuis le menu. Il faudra juste modifier le nom de la classe et
le type dans chaque classe où on veut être ammené, ici ce type est MenuView.

ON ne peut pas en faire une classe abstraite car nos boutons on besoin d'être en static et que overiide et static
ne sont pas compatible.

package INSA.TD.controllers.implementation;

import INSA.TD.views.implementation.CompetenceView;
import INSA.TD.views.implementation.MenuView;
import javafx.stage.Stage;
import INSA.TD.views.implementation.TypeView;

public class ButtonController {

    public void goToClasseView(Stage stage, boolean modifiable, boolean autorisation){
        MenuView nStage = new MenuView(stage,modifiable,autorisation);
    }

    public void goToModifier(Stage stage,  boolean modifiable, boolean autorisation){
        MenuView nStage = new MenuView(stage, true,autorisation);
    }

    public void goToSauvegarder(Stage stage, boolean modifiable, boolean autorisation){
        MenuView nStage = new MenuView(stage,false,autorisation);
        // ajouter système pour sauvegarder les modifications
    }
}
